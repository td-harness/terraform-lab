apiVersion: v1
kind: Secret
metadata:
  name: harness-example
stringData:
  sasljaas: ${secrets.getValue("sasljaas")}
  simple_secret_test: ${secrets.getValue("sshtest")}
  sasljaas2: ${secrets.getValue("sasljaas")}
